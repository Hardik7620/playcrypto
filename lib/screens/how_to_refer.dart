import 'package:flutter/material.dart';

/// Stub - how to refer page removed for consultant build
class HowToRefer extends StatelessWidget {
  const HowToRefer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Not available')),
    );
  }
}
