import 'package:flutter/material.dart';

/// Stub - game launch functionality removed for consultant build
class GameLaunchCode extends StatelessWidget {
  static const routeName = '/game-launch';
  const GameLaunchCode({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Game launch not available')),
    );
  }
}
