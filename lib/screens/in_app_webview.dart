import 'package:flutter/material.dart';

/// Stub - in-app webview removed for consultant build
class InAppWebViewCustom extends StatelessWidget {
  final String launchUrl;
  const InAppWebViewCustom({Key? key, required this.launchUrl}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Not available')),
    );
  }
}
